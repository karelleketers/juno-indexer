{
  "name": "@abstract-os/cw20-subgraph",
  "version": "0.0.1",
  "description": "This project indexes events for cw20 tokens and stores them to a database.",
  "main": "dist/index.js",
  "scripts": {
    "build": "subql build",
    "codegen": "subql codegen",
    "codegen:event": "yarn codegen --file project-event.yaml",
    "codegen:msg": "yarn codegen --file project-message.yaml",
    "start:docker": "docker-compose pull && docker-compose up --remove-orphans",
    "dev": "subql codegen && subql build && docker-compose pull && docker-compose up --remove-orphans",
    "prepack": "rm -rf dist && npm run build",
    "test": "jest"
  },
  "homepage": "https://github.com/Abstract-OS/cw20-subgraph",
  "repository": "github:Abstract-OS/cw20-subgraph",
  "files": [
    "dist",
    "schema.graphql",
    "project.yaml",
    "project-event.yaml",
    "project-message.yaml.yaml"
  ],
  "author": "karelleketers",
  "license": "MIT",
  "devDependencies": {
    "@cosmjs/stargate": "^0.28.9",
    "@subql/cli": "latest",
    "typescript": "^4.7.4"
  },
  "dependencies": {
    "@subql/types-cosmos": "latest",
    "@terra-money/terra.js": "^3.1.7",
    "@types/node": "^17.0.21",
    "pino": "^7.8.0",
    "ts-proto": "^1.112.1",
    "tslib": "^2.3.1"
  }
}
